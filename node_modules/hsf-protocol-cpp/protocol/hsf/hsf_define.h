#ifndef HSF_HSF_DEFINE_H
#define HSF_HSF_DEFINE_H

#include <stdint.h>
#include <string.h>
#include <cstdio>
#include "utils.h"        // 鹿芦鹿虏鹿陇戮脽
#include "tb_remoting.h"  // tb-remoting 脥酶脗莽虏茫
#include "objects.h"
namespace hsf {
#define likely(x)                   __builtin_expect(!!(x), 1)
#define unlikely(x)                 __builtin_expect(!!(x), 0)

/*
 * HSFRequest
 */
#define HSF_REQUEST_CLASSNAME "com.taobao.hsf.domain.HSFRequest"
#define HSF_REQUEST_CLASSNAME_SIZE sizeof(HSF_REQUEST_CLASSNAME) - 1

/*
 * HSFResponse
 */
#define HSF_RESPONSE_CLASSNAME "com.taobao.hsf.domain.HSFResponse"
#define HSF_RESPONSE_CLASSNAME_SIZE sizeof(HSF_RESPONSE_CLASSNAME) - 1

/** HSF 脟毛脟贸 */
typedef struct hsf_request {
  int          entry_id;
  std::string* service_signature;
  std::string* method_name;
  std::string* args_sig;
  List*        args;
} hsf_request;

/** HSF 脧矛脫娄 */
typedef struct hsf_response {
  uint8_t      is_error;
  std::string* error_msg;
  Object*      app_response;
} hsf_response;
}

#endif
